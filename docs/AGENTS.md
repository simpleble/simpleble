<!-- berth:start -->
## Berth

This project is built and deployed with Berth. Before changing deployment
configuration or workflows, run `berth app docs`.

`.berth.toml` is the authoritative project deployment manifest.
Secret values belong only in the manifest's configured local secrets file.
<!-- berth:end -->
