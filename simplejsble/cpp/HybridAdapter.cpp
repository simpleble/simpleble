#include "HybridAdapter.hpp"

namespace margelo::nitro::simplejsble {

    std::string HybridAdapter::greet(const std::string& name) {
        return "Hello, " + name + "!";
    }

} // namespace margelo::nitro::simplejsble