# Files copied by prepack script (should not be committed)
simpleble/
cmake/
dependencies/
VERSION
simpledroidbridge/

# iOS build intermediates and binaries
ios/build_*/
ios/simpleble_*/
ios/**/*.a
ios/**/*.xcframework

# macOS build intermediates (XCFramework is committed for npm distribution)
macos/build_*/
macos/simpleble_*/

# Nitrogen generated files (regenerated on each `npx nitrogen` run)
nitrogen/
